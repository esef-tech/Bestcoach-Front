// src/COMPONENTS/LazyStripeAndChat.jsx
const LazyStripeAndChat = () => {
  // Keep this component intentionally inert.
  // Stripe is loaded only where/when payment is actually initialized.
  return null;
};

export default LazyStripeAndChat;
